image: node:22
stages:
  - build

build_job:
  stage: build
  script:
    - echo "first step"
    - echo "build stage running successfully"
    - node -v
    - npm -v
    - npm install
    - npm run build
    - echo "last step"
    - echo "this is the end of build stage"
